
  to mark '<TAB>
  to mark ab'<TAB>
explodes with bash -c ' parse error
  to mark \'<TAB> works,
but it would be nice for above not to explode.
A simple printf %q does not solve this.

als git gi*
  (don't do that)
explodes too (when expanding to some stuff):
  Mixed up mess in amux
  Not a command, ++
basically no argument was picked up before [mark] [dir]


